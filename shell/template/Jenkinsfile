pipeline {
    agent any
    environment {
       HOME = "${env.WORKSPACE}"
    }
    stages {
        stage('Build') {
            steps {
                sh shell/play/publish_all.sh --param param_api_image_tag=play
            }
        }
    }
}