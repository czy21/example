plugins {
    id 'java-gradle-plugin'
    id 'groovy'
    id 'maven-publish'
}

group = 'com.team'
version = '1.0.0'

gradlePlugin {
    plugins {
        simplePlugin {
            id = 'com.team.gradle.common'
            implementationClass = 'com.team.gradle.CommonPlugin'
        }
    }
}
