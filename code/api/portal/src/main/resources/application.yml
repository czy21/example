server:
  port: 8080
spring:
  application:
    name: demo-portal
  config:
    import: nacos:${spring.application.name}.yml
  cloud:
    nacos:
      server-addr: nacos.cluster.com:8848
      config:
        file-extension: yml
        namespace: DEMO