server:
  port: 8080
spring:
  application:
    name: erp-gateway
  config:
    import: consul:consul.cluster2.com:80
  cloud:
    consul:
      config:
        format: yaml
        prefixes: ${ENV_NAME}
        default-context: ${spring.application.name}
  profiles:
    active: local
