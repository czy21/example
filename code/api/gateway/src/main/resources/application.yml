spring:
  application:
    name: demo-gateway
  config:
    import: optional:consul:consul.cluster.com:80
  cloud:
    consul:
      config:
        acl-token: anonymous
        format: YAML
        prefixes:
          - "demo-gateway"
  profiles:
    active: local