spring:
  application:
    name: erp-gateway
  cloud:
    consul:
      host: consul.cluster2.com
      port: 80
      config:
        acl-token: anonymous
        format: YAML
        prefixes:
          - "dev-${spring.application.name}"
  profiles:
    active: local