<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baidu.fsg.uid.worker.dao.WorkerNodeDAO">
    <resultMap id="workerNodeRes"
               type="com.baidu.fsg.uid.worker.entity.WorkerNodeEntity">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="host_name" jdbcType="VARCHAR" property="hostName"/>
        <result column="port" jdbcType="VARCHAR" property="port"/>
        <result column="type" jdbcType="INTEGER" property="type"/>
        <result column="launch_date" jdbcType="DATE" property="launchDate"/>
        <result column="sequence_value" jdbcType="BIGINT" property="sequenceValue"/>
        <result column="added_time" jdbcType="TIMESTAMP" property="addedTime"/>
        <result column="modified_time" jdbcType="TIMESTAMP" property="modifiedTime"/>
    </resultMap>

    <insert id="addWorkerNode" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.baidu.fsg.uid.worker.entity.WorkerNodeEntity">
		INSERT INTO WORKER_NODE
		(host_name,
		port,
		type,
		launch_date,
		sequence_value,
		added_time,
		modified_time)
		VALUES (
		#{hostName},
		#{port},
		#{type},
		#{launchDate},
		#{sequenceValue},
		NOW(),
		NOW())
	</insert>

    <select id="getWorkerNodeByHostPort" resultMap="workerNodeRes">
		SELECT
		id,
		host_name,
		port,
		type,
		launch_date,
		added_time,
		modified_time
		FROM
		WORKER_NODE
		WHERE
		host_name = #{host} AND PORT = #{port}
	</select>

    <update id="updateWorkerNode">
        update worker_node A,
        (SELECT max(modified_time) max_day FROM worker_node) B
        <set>
            A.sequence_value=#{sequenceValue,jdbcType=BIGINT}
        </set>
        WHERE A.modified_time = B.max_day
    </update>
</mapper>